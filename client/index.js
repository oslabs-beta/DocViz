import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => <h1>Welcome to DocViz!</h1>;

// Create a root and render the App component
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);


